// const binarySearch = (array: Array<number>, value: number) => {
//     array.sort() !== array ? false : () => {
//         const left = array[0]
//         const right = array.indexOf(array.length -1)
//         const middle = Math.floor((left + right) / 2)

//         while(middle !== value && left < right) {
//             if(value < array[middle])
//         }
//     }
// }
